name: 🔄 自动同步模型索引（带缩略图）

on:
  push:
    paths:
      - 'models/**'
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-index:
    runs-on: ubuntu-latest
    
    steps:
      # 步骤1：获取代码
      - name: 📥 获取代码
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      # 步骤2：设置Python环境
      - name: 🐍 设置Python环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'

      # 步骤3：安装依赖
      - name: 📦 安装依赖
        run: |
          echo "正在安装依赖..."
          sudo apt-get update
          sudo apt-get install -y libjpeg-dev zlib1g-dev libpng-dev
          pip install Pillow requests
          echo "✅ 依赖安装完成"

      # 步骤4：运行索引生成脚本
      - name: 🚀 运行索引生成脚本
        run: |
          echo "开始生成索引..."
          python scripts/generate_index_with_thumbnails.py
          
          echo "生成的索引预览："
          if [ -f "models/index.json" ]; then
            head -20 models/index.json
          else
            echo "❌ 索引文件未生成"
            exit 1
          fi

      # 步骤5：提交更新
      - name: 💾 提交更新
        run: |
          echo "检查更改..."
          
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          
          git add models/index.json
          
          if git diff --cached --quiet; then
            echo "🔄 没有变化，跳过提交"
          else
            echo "📝 检测到变化，正在提交..."
            git commit -m "🔄 自动更新模型索引 v2.0 [skip ci]"
            git push origin main
            echo "✅ 提交成功！"
          fi

      # 步骤6：完成报告
      - name: 📋 完成报告
        if: always()
        run: |
          echo "🎉 工作流执行完成"
          echo "📅 完成时间: $(date)"
          echo "🏃 工作流ID: ${{ github.run_id }}"
